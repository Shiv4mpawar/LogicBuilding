#include<stio.h>
#include<stdlib.h>

typedef struct Node
{
    int no;
    struct Node *next;
}NODE, *PNODE, **PPNODE;    // is called short hand sentence.


int main()
{
    PNODE First = NULL;
    

    return 0;
}